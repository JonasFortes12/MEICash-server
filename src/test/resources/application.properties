spring.application.name=meicash_test
spring.datasource.url=jdbc:postgresql://localhost:${MEICASH_TEST_POSTGRES_PORT}/${MEICASH_TEST_POSTGRES_DB}
spring.datasource.username=${MEICASH_TEST_POSTGRES_USER}
spring.datasource.password=${MEICASH_TEST_POSTGRES_PASSWORD}
spring.sql.init.mode=always